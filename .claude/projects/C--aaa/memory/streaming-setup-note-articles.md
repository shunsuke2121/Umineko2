---
name: streaming-setup-note-articles
description: User builds a Japanese live-streaming setup on Windows and sells troubleshooting writeups on Note
metadata: 
  node_type: memory
  type: project
  originSessionId: cc4d3684-17a4-4f3d-b64d-9f7d3783a98f
---

User is assembling a Japanese live-streaming environment on Windows 10 Home (64bit): 棒読みちゃん (BouyomiChan, 32bit TTS reader), VOICEVOX + SAPIForVOICEVOX32 bridge, kilin's HTML5 コメントジェネレーター (HCG, at C:\hcg_0_0_8a), OBS browser sources, Twitch. They monetize fixes by writing them up as paid articles on **Note (note.com)**.

**Why:** They explicitly said "Noteで売ります" about a solved issue.

**How to apply:** When a problem is solved, offer (or provide) a detailed, reproducible, article-ready writeup in Japanese — symptom → diagnosis steps with evidence → root cause → fix → checklist. They value the *process*, not just the answer. Keep diagnoses concrete (actual commands/outputs).

Solved so far: SAPIForVOICEVOX DllNotFoundException → missing 32bit VC++ 2015-2022 x86 redist. HCG not rendering → opened via file:// (local AJAX blocked) + dead createjs CDN (404) → vendored createjs/jQuery locally and rewrote script srcs. MikuMikuDance v932 x64 (C:\aaa\MikuMikuDance_v932x64\) "サイドバイサイド構成が正しくない" launch error → Application/SideBySide event log showed missing Dependent Assembly Microsoft.VC90.OpenMP amd64 v9.0.21022.8 → fix = install **Visual C++ 2008 SP1 Redistributable x64** (vcredist_x64.exe, MS Download id=2092, installs 9.0.30729 whose publisher policy redirects the pinned 9.0.21022.8). Verified WinSxS now has amd64_microsoft.vc90.openmp...9.0.30729.6161 and no new SxS errors. SECOND gotcha: MMD must be launched with WorkingDirectory = its own folder or it silently exits (code 0, no window) — double-clicking the exe is fine; a shortcut needs "作業フォルダ/Start in" set to C:\aaa\MikuMikuDance_v932x64. This is its own article-worthy fix.

Custom overlays built for them (self-contained, no CDN): C:\aaa\stream_overlays\ (walking-cat.html — cute pixel-art cat, ?color= / ?cats= variants; sub-alert.html — pixel sub alert w/ chiptune; hiro-walk.html — 2026-06, pixel-art 篠澤広/Shinosawa Hiro walking back-and-forth, 22x29 dots, same engine as the cat: LEFT[] 11-col left-half mirrored for symmetry + legsA/legsB walk frames + bob, PAL palette, ?px= / ?speed= params). Pixel-art workflow tip: verify sprite grids by rendering them to PNG with a PowerShell System.Drawing script and Read-ing the image (case-sensitive palette needs Dictionary[string,string], not a PS hashtable — H/h collide). Niconico-style scrolling comments: C:\hcg_0_0_8a\CommentGenerator0.0.8a\danmaku.html (reads comment.xml via XHR, ?demo=1 / ?debug=1).

KEY GOTCHA — OBS browser source canNOT read a sibling local file (comment.xml) over file:// — both fetch() AND XMLHttpRequest are blocked. fetch() can't read file:// at all (even in OBS CEF). Fix: run a local HTTP server and point OBS's URL field (uncheck "Local file") at http://localhost:8000/... Made a launcher: C:\hcg_0_0_8a\CommentGenerator0.0.8a\配信サーバ起動.bat (+ danmaku-server.ps1, a PowerShell HttpListener serving that folder on :8000). This also fixes HCG CommentGenerator.html. Overlays that DON'T read files (walking-cat, sub-alert) work fine via plain file://.

2026-06: Streaming 学園アイドルマスター (gakumas, portrait mobile game) on Twitch. Built a full-screen 1920x1080 OBS overlay at C:\aaa\gakumas_overlay.html (+ README_overlay.md) — centered transparent "window" for the vertical game, left=comment feed, right=streamer header/now-playing/clock/follower-goal/recent-follows, bottom ticker; Twitch-purple + idol pastel. Keys: G=alignment guide, D=demo toggle, C=settings. **Comment source decision: switching from MultiCommentViewer to わんコメ (OneComme).** Verified: OneComme's raw WebSocket API is now NON-PUBLIC — correct path is the official OneSDK template SDK: load http://localhost:11180/templates/preset/__origin/js/onesdk.js, then OneSDK.setup({protocol:'local',port:11180,permissions:['comments','connected']}) → OneSDK.ready() → OneSDK.subscribe({action:'comments',callback}) → OneSDK.connect(); comment fields at comment.data.{name,displayName,comment,profileImage,isOwner,isModerator,isSubscriber,hasGift}. Overlay falls back to demo comments if OneSDK absent (わんコメ not running). Consolidation plan: Twitch → わんコメ → both overlay (display) AND 棒読みちゃん (read-aloud via 127.0.0.1:50001, selected in わんコメ 読み上げ settings). NOTE: there is NO standard WebSocket-out plugin for MultiCommentViewer — its normal OBS route is コメジェネ (CommentGenerator), file-based not WS.

2026-06: Built a STANDBY/"まもなく配信開始" screen at C:\aaa\gakumas_standby.html (+ README_standby.md): full pastel scene, central window matching the live overlay (--game-w/h/top identical), てまぬい "peeks" from the window over a sill that hides its lower body; left=title+countdown (CONFIG.targetClock or countdownMinutes), right=今日やること list + Twitch/X handles, floating notes/sparkles. Keys G=align guide, R=restart countdown. Asset set: C:\aaa\学マスぬいセット\ (てまぬい.pmx + ことぬい/さきぬい/広ぬい/美鈴ぬい/千奈ぬい, TEX/*.tga, expression sheets). **CRITICAL LICENSE (制作:シャド, X:@shadoners, 二次創作, readme is Shift-JIS):** ⑤商用利用・AI収益化・LoRA二次配布 禁止 (so monetized Twitch/paid-Note use is a concern — flag it); ⑥ MMD以外での使用・変換 NG → do NOT load .pmx into three.js/Web3D; must render IN MMD then capture. So standby uses Method A (MMD window → OBS window-capture + chroma key, placed in center window behind the HTML frame) or Method B (export transparent PNG from MMD → assets/temanui_peek.png; HTML auto-shows it, else CSS placeholder). Camera FOV 15 recommended (readme). Credit shown bottom-right of standby. Always respect creator terms — relevant to user's reputation since they sell Note articles.

2026-06 DONE (Method B, via computer-use driving MMD): produced C:\aaa\assets\temanui_peek.png (transparent, 447x461, autocropped). てまぬい=月村手毬 (cool/mysterious, dark teal hair, olive eyes). Process & KEY GOTCHAS (article-worthy, reuse for the other 5 nui): (1) MMD's ファイル→画像ファイルに出力 ALREADY outputs an alpha-transparent PNG (background = A0) — no green-screen/two-pass matting needed. (2) BUT 座標軸表示 (表示V menu) renders a pure-green vertical axis line (0,255,0) + ground grid INTO the PNG — must turn it OFF first or it contaminates the cutout. (3) 出力サイズ (表示V→出力サイズ) is capped by monitor height (~1080) — 800x1100 errors "このPCで処理できる大きさを超えています"; used 720x1000. (4) 地面影表示 OFF to avoid ground shadow. (5) Camera doesn't persist unless you click 登録 in カメラ操作 after editing カメラ中心/距離/視野角 — switching モデル編⇄カメラ編 reverts unregistered camera. Used カメラ中心 Y=2, 距離=13, 視野角=15. (6) Expression set in 表情操作: まゆ にこり ~0.9 + リップ ω 0.85 (cute cat-mouth). MMD launched on secondary monitor TOSHIBA-TV (computer-use switch_display). Autocrop done with C:\aaa\make_crop.ps1 (PowerShell System.Drawing — no Python/ImageMagick on this PC). Project saved C:\aaa\temanui_standby.pmm; uncropped backup temanui_clean.png.
